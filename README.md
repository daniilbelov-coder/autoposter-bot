# 🤖 Telegram Autoposter Bot

Автоматический бот для публикации сообщений в Telegram каналы по расписанию с интерактивными командами и еженедельной рассылкой календаря.

## ✨ Возможности

### Автопостинг в каналы
- 🕐 **11:00 МСК** - первая отправка
- 🕐 **13:00 МСК** - вторая отправка
- 🕐 **16:00 МСК** - третья отправка
- 🎯 Умный выбор сообщений на основе частоты
- 🚫 Предотвращение конфликтов между сообщениями
- 📸 Поддержка одиночных фото и media groups

### Интерактивный бот
- 💬 Команды: `/start`, `/help`, `/status`
- 🔘 Кнопки для управления подпиской
- 📊 Статистика подписчиков

### Еженедельная рассылка
- 📅 **Понедельник 10:00 МСК** - автоматическая рассылка
- 📊 Excel календарь с планом публикаций на неделю
- 📧 Рассылка всем подписчикам

## 🚀 Быстрый старт

### 1. Деплой на Railway (рекомендуется)

**Шаг 1:** Форкните репозиторий или используйте напрямую  
**Шаг 2:** Перейдите на [railway.app](https://railway.app/)  
**Шаг 3:** New Project → Deploy from GitHub repo  
**Шаг 4:** Выберите этот репозиторий  

**Шаг 5:** Добавьте переменные окружения в Settings → Variables:

```env
BOT_TOKEN=ваш_токен_от_BotFather
CHANNEL_IDS=-1001234567890,-1009876543210
TIMEZONE=Europe/Moscow
DATABASE_PATH=bot_data.db
ADMIN_PASSWORD=ваш_надежный_пароль
```

**Шаг 6:** Railway автоматически задеплоит бота!

### 2. Локальный запуск

```bash
# Клонировать репозиторий
git clone https://github.com/daniilbelov-coder/autoposter-bot.git
cd autoposter-bot

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл
cp .env.example .env
# Отредактировать .env (добавить токен и ID каналов)

# Запустить бота
python bot.py
```

## ⚙️ Настройка

### Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

### Получение ID канала

**Способ 1: Через @userinfobot**
1. Добавьте вашего бота в канал как администратора с правом "Публикация сообщений"
2. Откройте [@userinfobot](https://t.me/userinfobot)
3. Перешлите любое сообщение из вашего канала боту
4. Скопируйте ID (начинается с `-100`)

**Способ 2: Через @getmyid_bot**
1. Добавьте [@getmyid_bot](https://t.me/getmyid_bot) в канал
2. Бот автоматически отправит ID

### Настройка сообщений

Отредактируйте `messages.json`:

```json
{
  "id": 1,
  "title": "Название сообщения",
  "frequency": "weekly",
  "frequency_days": 7,
  "text": "Текст с поддержкой <b>HTML</b>",
  "photos": ["photos/photo.jpg"],
  "do_not_schedule_same_day_with": [2, 3]
}
```

**Поля:**
- `id` - уникальный номер
- `title` - название для логов
- `frequency` - описание частоты (weekly, monthly)
- `frequency_days` - минимум дней между публикациями
- `text` - текст сообщения (HTML разметка)
- `photos` - массив путей к фото
- `do_not_schedule_same_day_with` - ID конфликтующих сообщений

## 💬 Команды бота

### Для пользователей:

- `/start` - Главное меню и подписка на расписание
- `/help` - Справка по командам
- `/status` - Проверить статус подписки

### Кнопки:
- 📅 **Подписаться на расписание** - получать календарь каждый понедельник
- 📅 **Получить календарь** - получить календарь прямо сейчас (без подписки)
- 📤 **Отправить пост (Админ)** - ручная отправка поста (требуется пароль)
- ❌ **Отписаться** - отменить рассылку
- ℹ️ **Информация** - о боте и функциях

## 🔐 Админ-функция: Ручная отправка постов

### Описание

Администратор бота может вручную отправлять посты в каналы вне обычного расписания. Эта функция защищена паролем и доступна через кнопку в главном меню.

### Как настроить

1. **Установите админ-пароль в `.env` файле:**

```env
ADMIN_PASSWORD=MySecureP@ssw0rd2026!
```

⚠️ **Важно:**
- Используйте сложный пароль (минимум 12 символов)
- Пароль чувствителен к регистру
- НЕ храните файл `.env` с реальным паролем в git
- Пример сложного пароля: `MySecureP@ssw0rd2026!`

2. **На Railway добавьте переменную:**

Перейдите в Settings → Variables и добавьте:
```
ADMIN_PASSWORD=ваш_надежный_пароль
```

### Как использовать

1. **Откройте бота и нажмите `/start`**
2. **Нажмите кнопку "📤 Отправить пост (Админ)"**
3. **Введите пароль администратора**
4. **Выберите пост из списка** (введите номер)
5. **Пост будет отправлен во все каналы**

### Пример использования

```
Вы: /start

Бот: 👋 Привет!
     [Кнопка: 📤 Отправить пост (Админ)]

Вы: *нажимаете кнопку*

Бот: 🔐 Админ-панель
     Введите пароль администратора:

Вы: MySecureP@ssw0rd2026!

Бот: 🔐 Админ-панель: Выбор поста для отправки
     
     1. Доставки в тёмное время 🌙
     2. Заряжен перед слотом 🔋
     3. Аккуратность на доставках
     ...
     27. База знаний для курьеров
     
     Всего постов: 27
     
     Введите номер поста:

Вы: 5

Бот: ⏳ Отправка поста...
     
     ✅ Пост успешно отправлен во все каналы!
     📝 Пост: [Название поста]
     📊 Статистика:
       • Успешно: 2/2
       • Ошибок: 0/2
```

### Отмена операции

В любой момент можно использовать `/cancel` для отмены операции.

### Безопасность

- ✅ Пароль хранится только в `.env` файле
- ✅ Неудачные попытки входа логируются
- ✅ Таймаут диалога: 5 минут
- ✅ Кнопка видна всем, но доступ только с паролем
- ✅ Все отправки помечены префиксом `[АДМИН]` в логах

### Логи админ-отправок

Все ручные отправки записываются в базу данных с пометкой `[АДМИН]`:

```
[АДМИН] Отправка сообщения 5 (Название поста)
[АДМИН] Отправка в канал -1001234567890...
[АДМИН] Отправка завершена: успешно 2, ошибок 0
```

## 📊 Структура проекта

```
autoposter_bot/
├── bot.py                  # Главный модуль бота
├── config.py              # Конфигурация
├── database.py            # SQLite база данных
├── handlers.py            # Обработчики команд
├── message_selector.py    # Логика выбора сообщений
├── scheduler.py           # Планировщик задач
├── schedule_generator.py  # Генератор Excel календаря
├── messages.json          # База сообщений
├── requirements.txt       # Python зависимости
├── .env.example          # Пример конфигурации
├── .gitignore            # Игнорируемые файлы
├── Procfile              # Для Railway/Heroku
├── railway.json          # Конфигурация Railway
├── runtime.txt           # Версия Python
└── photos/               # Фотографии для постов
```

## 🗄️ База данных

### Таблица `message_history`
Хранит историю всех отправок в каналы:
- ID сообщения и название
- Дата и время отправки
- ID канала
- Статус (успех/ошибка)

### Таблица `subscribers`
Управление подписчиками:
- User ID и контактные данные
- Дата подписки
- Статус активности
- Время последней отправки расписания

## 🔧 Расширенные настройки

### Изменение расписания

Отредактируйте `config.py`:

```python
POSTING_TIMES = [
    {'hour': 11, 'minute': 0},
    {'hour': 13, 'minute': 0},
    {'hour': 16, 'minute': 0},
]
```

### Изменение времени рассылки календаря

Отредактируйте `scheduler.py`:

```python
# Вместо понедельника 10:00
trigger = CronTrigger(
    day_of_week='mon',  # mon-sun
    hour=10,
    minute=0,
    timezone=TIMEZONE
)
```

### Добавление нескольких каналов

В `.env`:
```env
CHANNEL_IDS=-1001234567890,-1009876543210,-1005555555555
```

## 📈 Мониторинг

### Просмотр логов (Railway)

```bash
railway logs
```

### Проверка подписчиков (локально)

```python
from database import db

# Количество подписчиков
print(db.get_subscribers_count())

# Список подписчиков
for sub in db.get_active_subscribers():
    print(f"@{sub['username']} - {sub['subscribed_at']}")
```

## 🐛 Решение проблем

### Бот не отвечает на команды

1. Проверьте логи в Railway/локально
2. Убедитесь, что токен правильный
3. Проверьте, что бот запущен

### Не отправляются сообщения в канал

1. Убедитесь, что бот добавлен в канал как администратор
2. Проверьте право "Публикация сообщений"
3. Убедитесь, что ID канала правильный (начинается с `-100`)

### "Chat not found"

- Неправильный ID канала
- Бот не добавлен в канал

### "No rights to send messages"

- Дайте боту право "Публикация сообщений" в настройках канала

### База данных заблокирована

- Используйте локальную файловую систему (не NFS)
- На Railway все работает автоматически

## 🔒 Безопасность

- ⚠️ Никогда не коммитьте `.env` файл
- ⚠️ Храните токен бота в секрете
- ⚠️ Используйте сложный админ-пароль (минимум 12 символов)
- ⚠️ Не делитесь админ-паролем с посторонними
- ✅ `.env` уже добавлен в `.gitignore`
- ✅ Используйте переменные окружения на Railway
- ✅ Админ-операции логируются для аудита

```bash
chmod 600 .env  # Ограничить доступ к файлу
```

## 📦 Зависимости

```
python-telegram-bot==21.0  # Telegram Bot API
APScheduler==3.10.4        # Планировщик задач
python-dotenv==1.0.1       # Загрузка .env
pytz==2024.1               # Часовые пояса
openpyxl==3.1.2           # Генерация Excel
```

## 🎯 Примеры использования

### Пример 1: Подписка пользователя

```
Пользователь: /start

Бот: 👋 Привет!
     📅 Подпишитесь на еженедельное расписание
     
     [Кнопка: Подписаться на расписание]

Пользователь: *нажимает кнопку*

Бот: ✅ Вы успешно подписаны!
     Каждый понедельник в 10:00 МСК будете получать календарь
```

### Пример 2: Календарь в Excel

| № | Дата | День | Время | Название | Частота | Фото |
|---|------|------|-------|----------|---------|------|
| 1 | 20.01 | Пн | 11:00 | Стандарты уборки | Месяц | ✓ (1) |
| 2 | 20.01 | Пн | 13:00 | Качество уборки | Неделя | ✓ (1) |

## 🔄 Обновление

### Локально

```bash
git pull origin main
pip install -r requirements.txt
python bot.py
```

### Railway

Railway автоматически обновляется при push в GitHub

## 📞 Поддержка

**GitHub:** https://github.com/daniilbelov-coder/autoposter-bot

При возникновении проблем:
1. Проверьте логи
2. Убедитесь в правильности настроек
3. Проверьте права бота в каналах

## 📄 Лицензия

MIT License

---

**Разработано для автоматизации публикаций в Telegram** 🚀

**Версия:** 2.0  
**Дата:** 2026-01-15
