# Telegram Autoposter Bot

Автоматизированный бот для публикации сообщений в Telegram каналы по расписанию.

## Возможности

- ✅ Автоматическая публикация сообщений в заданное время (11:00, 13:00, 16:00 МСК)
- ✅ Умный выбор сообщений на основе частоты публикаций
- ✅ Предотвращение конфликтов между сообщениями
- ✅ Поддержка одиночных фото и media groups
- ✅ Отправка в несколько каналов одновременно
- ✅ SQLite база данных для отслеживания истории
- ✅ Подробное логирование всех операций

## 🚂 Быстрый деплой на Railway

[![Deploy on Railway](https://railway.app/button.svg)](https://railway.app/new/template?template=https://github.com/daniilbelov-coder/autoposter-bot)

**Самый простой способ запустить бота - деплой на Railway:**

1. Перейдите на [railway.app](https://railway.app/)
2. Подключите GitHub репозиторий: `daniilbelov-coder/autoposter-bot`
3. Добавьте переменные окружения (BOT_TOKEN, CHANNEL_IDS)
4. Готово! Бот запустится автоматически

📖 **Подробная инструкция:** [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md)

## Структура проекта

```
autoposter_bot/
├── bot.py                  # Главный модуль бота
├── config.py              # Конфигурация и настройки
├── database.py            # Работа с SQLite базой данных
├── message_selector.py    # Логика выбора сообщений
├── scheduler.py           # Планировщик задач
├── messages.json          # Файл с сообщениями
├── requirements.txt       # Зависимости Python
├── .env                   # Переменные окружения (создать вручную)
├── .env.example          # Пример файла с переменными
├── .gitignore            # Игнорируемые файлы
├── photos/               # Папка с фотографиями
└── README.md             # Документация
```

## Установка

### 1. Клонирование и подготовка окружения

```bash
# Перейти в директорию проекта
cd autoposter_bot

# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
# Для macOS/Linux:
source venv/bin/activate
# Для Windows:
# venv\Scripts\activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Создание Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### 4. Добавление бота в каналы

1. Добавьте бота в ваш Telegram канал
2. Дайте боту права администратора (минимум право на публикацию сообщений)
3. Получите ID канала:
   - Перешлите любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
   - Скопируйте ID канала (он начинается с `-100`)

### 5. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:

```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
CHANNEL_IDS=-1001234567890,-1009876543210
TIMEZONE=Europe/Moscow
DATABASE_PATH=bot_data.db
```

**Важно:** 
- `BOT_TOKEN` - токен от @BotFather
- `CHANNEL_IDS` - ID каналов через запятую (с минусом)
- `TIMEZONE` - часовой пояс (по умолчанию Moscow)

## Настройка сообщений

Отредактируйте файл `messages.json` для добавления своих сообщений:

```json
[
  {
    "id": 1,
    "title": "Название сообщения",
    "frequency": "weekly",
    "frequency_days": 7,
    "text": "Текст сообщения",
    "photos": ["photos/photo1.jpg"],
    "do_not_schedule_same_day_with": [2, 3]
  }
]
```

### Поля сообщения:

- `id` - уникальный идентификатор (число)
- `title` - заголовок для логов (строка)
- `frequency` - человекочитаемая частота (строка)
- `frequency_days` - минимальное количество дней между публикациями (число)
- `text` - текст сообщения, поддерживает HTML разметку (строка)
- `photos` - массив путей к фотографиям (может быть пустым)
- `do_not_schedule_same_day_with` - массив ID сообщений, с которыми нельзя публиковать в один день

### Примеры частоты:

- `frequency_days: 7` - каждую неделю
- `frequency_days: 14` - каждые 2 недели
- `frequency_days: 30` - каждый месяц

## Запуск бота

### Обычный запуск

```bash
python bot.py
```

### Запуск в фоновом режиме (Linux/macOS)

```bash
nohup python bot.py > bot.log 2>&1 &
```

### Использование screen (Linux/macOS)

```bash
screen -S autoposter
python bot.py
# Нажмите Ctrl+A, затем D для выхода
# Для возврата: screen -r autoposter
```

### Использование systemd (Linux)

Создайте файл `/etc/systemd/system/autoposter.service`:

```ini
[Unit]
Description=Telegram Autoposter Bot
After=network.target

[Service]
Type=simple
User=yourusername
WorkingDirectory=/path/to/autoposter_bot
Environment="PATH=/path/to/autoposter_bot/venv/bin"
ExecStart=/path/to/autoposter_bot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable autoposter
sudo systemctl start autoposter
sudo systemctl status autoposter
```

## Расписание отправки

По умолчанию бот отправляет сообщения в:
- 11:00 МСК
- 13:00 МСК
- 16:00 МСК

Для изменения расписания отредактируйте переменную `POSTING_TIMES` в файле `config.py`:

```python
POSTING_TIMES = [
    {'hour': 11, 'minute': 0},
    {'hour': 13, 'minute': 0},
    {'hour': 16, 'minute': 0},
]
```

## Логика работы

### Выбор сообщения

При каждом запланированном времени бот:

1. Загружает все сообщения из `messages.json`
2. Проверяет для каждого сообщения:
   - Прошло ли достаточно дней с последней публикации (`frequency_days`)
   - Нет ли конфликтов с уже опубликованными сегодня сообщениями
3. Из доступных сообщений выбирает случайное
4. Отправляет его во все настроенные каналы

### База данных

Бот автоматически создаёт SQLite базу данных (`bot_data.db`) для хранения:

- Истории всех публикаций
- Дат последних отправок
- Информации об ошибках

Структура таблицы `message_history`:

```sql
CREATE TABLE message_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    message_id INTEGER NOT NULL,
    message_title TEXT NOT NULL,
    sent_date DATE NOT NULL,
    sent_datetime TIMESTAMP NOT NULL,
    channel_id INTEGER NOT NULL,
    success BOOLEAN NOT NULL,
    error_message TEXT
)
```

## Тестирование

Для тестовой отправки создайте файл `test.py`:

```python
import asyncio
from bot import AutoPosterBot

async def test():
    bot = AutoPosterBot()
    
    # Тестовая отправка случайного сообщения
    await bot.test_send()
    
    # Или конкретного сообщения по ID
    await bot.test_send(message_id=1)

if __name__ == '__main__':
    asyncio.run(test())
```

Запустите:

```bash
python test.py
```

## Логирование

Бот выводит подробные логи всех операций:

- Инициализация компонентов
- Загрузка сообщений
- Проверка доступности
- Отправка в каналы
- Ошибки и предупреждения

Пример лога:

```
2026-01-15 11:00:00 - root - INFO - ==================================================
2026-01-15 11:00:00 - root - INFO - Запуск процесса отправки сообщения
2026-01-15 11:00:00 - root - INFO - Загружено 9 сообщений из messages.json
2026-01-15 11:00:00 - root - INFO - Сегодня уже отправлено сообщений: []
2026-01-15 11:00:00 - root - INFO - Выбрано сообщение 1 (Стандарты уборки)
2026-01-15 11:00:00 - root - INFO - Отправка сообщения 1 (Стандарты уборки) в 1 каналов
2026-01-15 11:00:01 - root - INFO - Сообщение с 1 фото отправлено в канал -1001234567890
2026-01-15 11:00:01 - root - INFO - Отправка завершена: успешно 1, ошибок 0
2026-01-15 11:00:01 - root - INFO - ==================================================
```

## Устранение неполадок

### Бот не отправляет сообщения

1. Проверьте, что бот добавлен в канал с правами администратора
2. Убедитесь, что токен бота правильный
3. Проверьте ID канала (должен начинаться с `-100`)
4. Посмотрите логи на наличие ошибок

### Файлы с фотографиями не найдены

1. Убедитесь, что пути в `messages.json` правильные
2. Проверьте, что папка `photos/` существует
3. Используйте относительные пути от корня проекта

### База данных заблокирована

Если используете NFS или сетевую файловую систему, SQLite может работать нестабильно. Рекомендуется использовать локальную файловую систему.

## Обновление

Для обновления зависимостей:

```bash
pip install --upgrade -r requirements.txt
```

## Безопасность

- ⚠️ Никогда не коммитьте файл `.env` в Git
- ⚠️ Храните токен бота в безопасности
- ⚠️ Используйте права доступа для защиты конфигурационных файлов

```bash
chmod 600 .env
```

## Лицензия

MIT License

## Поддержка

При возникновении проблем проверьте:
1. Логи бота
2. Настройки в `.env`
3. Права бота в каналах
4. Корректность `messages.json`

---

**Разработано для автоматизации публикаций в Telegram каналы** 🚀
