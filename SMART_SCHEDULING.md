# 🧠 Умное планирование календаря

## 📋 Проблема

Старая версия `schedule_generator.py` имела серьезные проблемы:
- ❌ **Дубликаты в один день**: одно сообщение могло появляться несколько раз в один день
- ❌ **Повторы за неделю**: сообщения с частотой "раз в 2 недели" публиковались несколько раз за неделю
- ❌ **Игнорировалась частота**: не учитывался `frequency_days`
- ❌ **Случайный выбор**: просто `random.choice()` для каждого слота

## ✅ Решение

Создана новая версия с умной логикой планирования, вдохновленной `content_planner.py`.

### Основные принципы

1. **Расчет приоритета сообщений**
   - Высокий приоритет для не опубликованных сообщений
   - Приоритет растет с количеством прошедших дней
   - Сообщения, срок которых еще не пришел, имеют приоритет 0

2. **Учет частоты публикаций**
   - `frequency_days = 7` → 1 раз в неделю
   - `frequency_days = 14` → раз в 2 недели (только в четную/нечетную неделю)
   - `frequency_days = 21` → раз в 3 недели
   - `frequency_days ≥ 28` → раз в месяц

3. **Двухпроходное планирование**
   - **Проход 1**: Размещение приоритетных сообщений с учетом частоты
   - **Проход 2**: Заполнение пустых слотов неиспользованными сообщениями

4. **Проверки при размещении**
   - ✅ Сообщение не было размещено в этот день
   - ✅ Сообщение не было размещено на этой неделе
   - ✅ Нет конфликтующих сообщений в этот день
   - ✅ Сообщение не размещалось в соседних днях (±2 дня)

5. **Балансировка загрузки**
   - Равномерное распределение постов по дням
   - Выбор дней с минимальной загрузкой
   - Случайный выбор между днями с одинаковой загрузкой

## 🔧 Технические детали

### Функция `can_add_post_to_day()`

Проверяет, можно ли добавить пост в указанный день:
```python
- Проверка 1: Пост уже запланирован на этот день
- Проверка 2: Конфликтующие посты в этот день
- Проверка 3: Тот же пост в соседних днях (±2 дня)
```

### Функция `calculate_message_priority()`

Рассчитывает приоритет сообщения:
```python
- Никогда не отправлялось: приоритет = 100
- Еще рано (days_passed < frequency_days): приоритет = 0
- Идеальное время (days_passed == frequency_days): приоритет = 50
- Просрочено (days_passed > frequency_days): приоритет = 50 + overdue_days * 5
```

### Функция `get_planned_posts_for_week_smart()`

Основная функция планирования:
1. Получить даты недели
2. Загрузить все сообщения
3. Рассчитать приоритеты
4. Определить частоту публикации для каждого сообщения
5. Проход 1: Разместить приоритетные сообщения
6. Проход 2: Заполнить пустые слоты
7. Преобразовать в список с временными слотами

## 📊 Результаты тестирования

### Проверка 1: Дубликаты в один день
```
✅ Дубликатов в один день НЕТ!
```

### Проверка 2: Повторы за неделю
```
✅ Повторов за неделю НЕТ!
```

### Проверка 3: Соседство сообщений
```
✅ Проблем с соседством НЕТ!
```

### Статистика
- **Всего сообщений**: 27
- **Постов в неделю**: 21 (3 поста × 7 дней)
- **Уникальных сообщений**: 21
- **Повторов**: 0

## 🔄 Сравнение версий

| Характеристика | Старая версия | Новая версия |
|----------------|---------------|--------------|
| Дубликаты в день | ❌ Возможны | ✅ Невозможны |
| Повторы за неделю | ❌ Возможны | ✅ Невозможны |
| Учет частоты | ❌ Нет | ✅ Да |
| Проверка соседства | ❌ Нет | ✅ ±2 дня |
| Конфликты | ❌ Не учитываются | ✅ Учитываются |
| Приоритезация | ❌ Нет | ✅ Да |
| Балансировка | ❌ Нет | ✅ Да |

## 📝 Примеры

### До (старая версия)
```
20.01 Вторник:
  11:00 Доставки с чужим аккаунтом ❌ (каждые 2 недели)
  13:00 Доставки с чужим аккаунтом ❌ (каждые 2 недели) ← ДУБЛИКАТ!
  16:00 База знаний для курьеров
```

### После (новая версия)
```
20.01 Вторник:
  11:00 Оплата заказов и чаевые (раз в месяц)
  13:00 Доставки в непогоду ☁️ (каждые 2 недели)
  16:00 Законы и предписания контрольных органов (раз в месяц)
```

## 🚀 Использование

Функции используются автоматически при:
1. Генерации календаря через команду `/schedule`
2. Автоматической еженедельной рассылке (понедельник, 10:00)
3. Нажатии кнопки "📅 Получить календарь"

## 🛠️ Техническая информация

### Файлы

- **schedule_generator.py** - новая версия с умной логикой
- **schedule_generator_old_backup.py** - бэкап старой версии
- **content_planner.py** - образец для вдохновения (не используется напрямую)

### Логирование

Все действия логируются:
```python
logger.info("Начало планирования на неделю: 27 сообщений доступно")
logger.info("Сообщений с приоритетом > 0: 27")
logger.info("Запланировано постов: 21")
logger.debug("Размещено: Медкнижка с собой на 2026-01-23")
logger.warning("Не удалось разместить сообщение 10")
```

## 🔍 Отладка

Для тестирования логики:
```bash
cd /Users/daniil-belov/Desktop/Cursor/autoposter_bot
./venv/bin/python3 test_schedule.py
```

Для проверки конкретных сообщений:
```python
from schedule_generator import calculate_message_priority
from datetime import datetime

message = {'id': 1, 'frequency_days': 14}
priority = calculate_message_priority(message, datetime.now())
print(f"Приоритет: {priority}")
```

## 📚 Алгоритм работы

```
1. Загрузить все сообщения из messages.json
2. Получить даты текущей недели (пн-вс)
3. Для каждого сообщения:
   a. Рассчитать приоритет (на основе last_sent_date)
   b. Определить, должно ли выйти на этой неделе (на основе frequency_days)
4. Отсортировать сообщения по приоритету (от высокого к низкому)
5. Проход 1: Размещение приоритетных сообщений
   - Для каждого сообщения с target_freq > 0:
     - Найти дни с минимальной загрузкой
     - Проверить все условия (can_add_post_to_day)
     - Разместить сообщение
     - Повторить target_freq раз
6. Проход 2: Заполнение пустых слотов
   - Для каждого дня с незаполненными слотами:
     - Найти сообщения, еще не размещенные на этой неделе
     - Проверить все условия
     - Добавить сообщение
7. Преобразовать расписание в список с временными слотами
8. Отсортировать по дате и времени
9. Сгенерировать Excel файл
```

## ⚠️ Известные ограничения

1. **Недельное планирование**: Планирование происходит только на текущую неделю
2. **Номер недели**: Для определения, должно ли выйти сообщение "раз в 2 недели", используется номер недели в году + ID сообщения
3. **Пустые слоты**: Если не хватает уникальных сообщений для заполнения всех слотов, остаются пустые

## 🎯 Будущие улучшения

- [ ] Планирование на несколько недель вперед
- [ ] Учет истории публикаций из базы данных для более точного определения недели
- [ ] Адаптивная балансировка: меньше постов в выходные
- [ ] Группы сообщений по темам
- [ ] Сезонная приоритезация
- [ ] A/B тестирование времени публикаций

---

**Версия**: 2.1  
**Дата**: 20.01.2026  
**Статус**: ✅ Протестировано и готово к использованию
