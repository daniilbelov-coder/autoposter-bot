# Руководство по обновлению бота

## 🎉 Что нового?

Бот успешно обновлен! Теперь он поддерживает:

1. ✅ **Новые сообщения** - 27 постов вместо 9
2. ✅ **Форматирование текста** - жирный, курсив, подчеркнутый, ссылки
3. ✅ **Видео файлы** - отправка видео в каналы
4. ✅ **Обновленные фотографии** - все медиа файлы обновлены

## 📋 Что было сделано

### 1. Обработка сообщений
- Исправлена структура `messages_new.json`
- Конвертирован Markdown в HTML форматирование
- Добавлены правильные пути к медиа файлам (`photos/`)
- Создан новый `messages.json` с 27 сообщениями

### 2. Поддержка видео
- Обновлен `bot.py` для отправки видео
- Добавлена автоматическая определение типа медиа
- Поддержка форматов: .mp4, .mov, .avi, .mkv

### 3. Форматирование текста
- Создан модуль `text_formatter.py`
- Автоматическая конвертация:
  - `**текст**` → `<b>текст</b>` (жирный)
  - `*текст*` → `<i>текст</i>` (курсив)
  - `__текст__` → `<u>текст</u>` (подчеркнутый)
  - `` `текст` `` → `<code>текст</code>` (код)
  - `[текст](url)` → `<a href="url">текст</a>` (ссылка)

## 🚀 Как запустить бота

### Обычный запуск
```bash
cd /Users/daniil-belov/Desktop/Cursor/autoposter_bot
source venv/bin/activate
python bot.py
```

### Тестирование (без отправки)
```bash
cd /Users/daniil-belov/Desktop/Cursor/autoposter_bot
source venv/bin/activate
python test_bot.py
```

## 📊 Статистика сообщений

- **Всего сообщений**: 27
- **С фотографиями**: 25
- **С видео**: 2
  - ID 28: "По зебре — всегда шагом 🦓" (`zebra_crossing.mov`)
  - ID 31: "Яндекс Про на разных языках" (`language.mp4`)

## 🔍 Проверка медиа файлов

Все медиа файлы проверены и найдены:
```bash
./venv/bin/python3 -c "
import json, os
data = json.load(open('messages.json'))
for m in data:
    for p in m.get('photos', []): 
        print(f'✅ {p}' if os.path.exists(p) else f'❌ {p}')
    for v in m.get('videos', []): 
        print(f'✅ {v}' if os.path.exists(v) else f'❌ {v}')
"
```

## 📝 Примеры сообщений

### С фото
```json
{
  "id": 22,
  "title": "Доставки с чужим аккаунтом ❌",
  "frequency_days": 14,
  "text": "<b>Доставки с чужим аккаунтом ❌</b>\n\n...",
  "photos": ["photos/account.jpg"]
}
```

### С видео
```json
{
  "id": 28,
  "title": "По зебре — всегда шагом 🦓",
  "frequency_days": 14,
  "text": "<b>По зебре — всегда шагом 🦓</b>\n\n...",
  "videos": ["photos/zebra_crossing.mov"]
}
```

## 🛠️ Вспомогательные скрипты

### `text_formatter.py`
Модуль для конвертации Markdown в HTML

### `test_bot.py`
Комплексное тестирование всех функций бота

### `convert_messages.py` и `fix_json.py`
Скрипты для конвертации `messages_new.json` (уже выполнено)

## ⚠️ Важные замечания

1. **Не удаляйте** файл `messages.json` - он содержит все обработанные сообщения
2. **Видео файлы** хранятся в папке `photos/` для единообразия
3. **Форматирование** применяется автоматически при загрузке сообщений
4. **Старые сообщения** можно восстановить из git истории

## 🐛 Если что-то не работает

1. Проверьте, что все медиа файлы на месте:
   ```bash
   ls -la photos/
   ```

2. Запустите тесты:
   ```bash
   ./venv/bin/python3 test_bot.py
   ```

3. Проверьте логи бота при запуске

## 📞 Поддержка

Все изменения задокументированы в `CHANGELOG.md`

Бот готов к работе! 🚀
