# Деплой на Railway 🚂

## Быстрый старт

### 1. Подготовка репозитория

✅ Репозиторий уже загружен на GitHub: **https://github.com/daniilbelov-coder/autoposter-bot**

### 2. Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app/)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите репозиторий **autoposter-bot**
5. Railway автоматически определит Python проект

### 3. Настройка переменных окружения

После создания проекта добавьте переменные окружения:

1. Перейдите в **Settings** → **Variables**
2. Добавьте следующие переменные:

```
BOT_TOKEN=ваш_токен_от_BotFather
CHANNEL_IDS=-1001234567890,-1009876543210
TIMEZONE=Europe/Moscow
DATABASE_PATH=bot_data.db
```

**ВАЖНО:** Замените `CHANNEL_IDS` на реальные ID ваших каналов!

### 4. Получение ID каналов

1. Добавьте бота в ваши Telegram каналы как администратора
2. Перешлите любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
3. Скопируйте ID канала (начинается с `-100`)
4. Обновите переменную `CHANNEL_IDS` в Railway

### 5. Деплой

Railway автоматически задеплоит проект после добавления переменных окружения.

Проверьте логи в разделе **Deployments** → **View Logs**

### 6. Мониторинг

В логах вы увидите:
```
INFO - Бот инициализирован
INFO - База данных инициализирована
INFO - Планировщик запущен
INFO - Запланировано задач: 3
```

Бот будет отправлять сообщения по расписанию:
- 11:00 МСК
- 13:00 МСК
- 16:00 МСК

## Структура файлов для Railway

### `railway.json`
Конфигурация Railway:
- Команда запуска: `python bot.py`
- Автоматический перезапуск при сбое

### `Procfile`
Альтернативная конфигурация для Heroku-совместимых платформ:
```
worker: python bot.py
```

### `runtime.txt`
Версия Python:
```
python-3.11.0
```

### `requirements.txt`
Зависимости проекта (уже настроены):
```
python-telegram-bot==21.0
APScheduler==3.10.4
python-dotenv==1.0.1
pytz==2024.1
```

## Хранение базы данных

Railway автоматически создаст volume для хранения `bot_data.db`:
- База данных сохраняется между деплоями
- История отправок не теряется

Если нужен внешний PostgreSQL:
1. Добавьте PostgreSQL плагин в Railway
2. Измените `database.py` для работы с PostgreSQL
3. Добавьте `psycopg2-binary` в `requirements.txt`

## Проверка работы

После деплоя:

1. **Проверьте логи** в Railway Dashboard
2. **Дождитесь времени отправки** (11:00, 13:00 или 16:00 МСК)
3. **Проверьте канал** - должно прийти сообщение

## Ручной триггер (опционально)

Для тестирования можно временно изменить расписание в `config.py`:

```python
POSTING_TIMES = [
    {'hour': 15, 'minute': 30},  # Через несколько минут
]
```

Закоммитьте и запушьте изменения - Railway автоматически передеплоит.

## Управление ботом

### Просмотр логов
```bash
railway logs
```

### Перезапуск
```bash
railway restart
```

### Остановка
```bash
railway down
```

## Стоимость

Railway предоставляет:
- **$5 бесплатно** каждый месяц
- Этот бот потребляет **минимум ресурсов**
- Легко укладывается в бесплатный план

## Troubleshooting

### Проблема: Бот не отправляет сообщения

**Решение:**
1. Проверьте переменную `CHANNEL_IDS`
2. Убедитесь, что бот администратор в каналах
3. Проверьте логи на ошибки

### Проблема: База данных не сохраняется

**Решение:**
1. Убедитесь, что `DATABASE_PATH=bot_data.db`
2. Railway автоматически создаст volume
3. Проверьте в Settings → Volumes

### Проблема: Неправильный часовой пояс

**Решение:**
1. Убедитесь, что `TIMEZONE=Europe/Moscow`
2. Перезапустите проект в Railway

## Полезные ссылки

- **Railway Dashboard:** https://railway.app/dashboard
- **Railway Docs:** https://docs.railway.app/
- **GitHub Repo:** https://github.com/daniilbelov-coder/autoposter-bot
- **Bot Token:** Получен от @BotFather
- **Support:** Railway Discord

---

## Чеклист перед деплоем

- ✅ Репозиторий на GitHub создан
- ✅ Токен бота получен от @BotFather
- ⚠️ ID каналов получены через @userinfobot (НУЖНО ДОБАВИТЬ!)
- ⚠️ Бот добавлен в каналы как администратор
- ⚠️ Переменные окружения добавлены в Railway
- ⚠️ Проект задеплоен и запущен

**После выполнения всех шагов бот будет работать автоматически!** 🎉
