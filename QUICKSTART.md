# Быстрый старт 🚀

## Пошаговая инструкция для запуска бота

### Шаг 1: Установка зависимостей

```bash
# Активировать виртуальное окружение (если есть)
source venv/bin/activate  # для macOS/Linux
# или
venv\Scripts\activate  # для Windows

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 2: Настройка бота

1. **Создайте бота в Telegram:**
   - Найдите [@BotFather](https://t.me/BotFather)
   - Команда: `/newbot`
   - Скопируйте полученный токен

2. **Получите ID канала:**
   - Добавьте бота в канал как администратора
   - Перешлите сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
   - Скопируйте ID канала (начинается с `-100`)

3. **Создайте файл .env:**
   ```bash
   cp .env.example .env
   ```

4. **Отредактируйте .env:**
   ```env
   BOT_TOKEN=ваш_токен_от_BotFather
   CHANNEL_IDS=-1001234567890,-1009876543210
   ```

### Шаг 3: Настройка сообщений

Отредактируйте `messages.json` под ваши нужды:
- Измените тексты сообщений
- Укажите правильные пути к фотографиям
- Настройте частоту публикаций

### Шаг 4: Тестирование

```bash
python test.py
```

Выберите режим тестирования и проверьте отправку.

### Шаг 5: Запуск бота

```bash
python bot.py
```

Бот будет автоматически отправлять сообщения в:
- **11:00** МСК
- **13:00** МСК
- **16:00** МСК

### Полезные команды

**Просмотр статистики:**
```bash
python stats.py
```

**Запуск в фоне (Linux/macOS):**
```bash
nohup python bot.py > bot.log 2>&1 &
```

**Остановка бота:**
- Нажмите `Ctrl+C` в терминале
- Или найдите процесс: `ps aux | grep bot.py`
- И завершите: `kill PID`

---

## Структура сообщения в messages.json

```json
{
  "id": 1,
  "title": "Название для логов",
  "frequency": "weekly",
  "frequency_days": 7,
  "text": "Текст сообщения с поддержкой HTML",
  "photos": ["photos/photo1.jpg", "photos/photo2.jpg"],
  "do_not_schedule_same_day_with": [2, 3]
}
```

- `frequency_days` - минимум дней между публикациями
- `photos` - массив путей к фото (может быть пустым)
- `do_not_schedule_same_day_with` - ID сообщений, с которыми нельзя публиковать в один день

---

## Часто задаваемые вопросы

**Q: Как изменить время публикаций?**  
A: Отредактируйте `POSTING_TIMES` в файле `config.py`

**Q: Как добавить новый канал?**  
A: Добавьте ID через запятую в `.env` файле: `CHANNEL_IDS=-100111,-100222,-100333`

**Q: Бот не отправляет сообщения**  
A: Проверьте:
- Бот добавлен в канал как администратор
- Токен и ID канала правильные
- Нет ошибок в логах

**Q: Как посмотреть, какие сообщения доступны?**  
A: Запустите `python stats.py` и выберите пункт 4

---

**Готово! Ваш бот работает! 🎉**

Для подробной информации смотрите [README.md](README.md)
